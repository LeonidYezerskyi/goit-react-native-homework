import React from "react";
import BottomTabNav from "../routes/bottomTabNav";

const HomeScreen = ({ navigation }) => {
  return <BottomTabNav navigation={navigation} />;
};

export default HomeScreen;
